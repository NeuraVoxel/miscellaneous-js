{
  "name": "hellonode-http2",
  "version": "1.0.0",
  "description": "Node.js HTTP/2服务器和客户端示例",
  "main": "server.js",
  "scripts": {
    "generate-cert": "node generate-cert.js",
    "start": "node server.js",
    "client": "node client.js",
    "setup": "npm install && npm run generate-cert",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "http2",
    "node",
    "server",
    "client",
    "example"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mime-types": "^2.1.35",
    "node-forge": "^1.3.1"
  }
}